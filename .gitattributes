# Force LF line endings on shell scripts and Dockerfile so they remain
# executable when checked out on Windows and then copied into a Linux container.
*.sh                text eol=lf
Dockerfile          text eol=lf
Dockerfile.*        text eol=lf
docker-compose*.yml text eol=lf
